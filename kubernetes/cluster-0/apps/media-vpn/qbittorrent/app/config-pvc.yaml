---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qb-config-pvc
  namespace: media-vpn
  labels:
    snapshot.home.arpa/enabled: "false"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: local-path
